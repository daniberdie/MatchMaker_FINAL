<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_initial"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/acces_background">


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1">

        <Button
            android:id="@+id/logout"
            android:layout_width="75dp"
            android:layout_height="75dp"
            android:layout_alignParentTop="true"
            android:backgroundTint="@color/common_google_signin_btn_text_light"
            android:foregroundTint="@color/colorAccent"
            android:foreground="@android:drawable/ic_lock_power_off"
            android:textSize="20dp" />

        <ImageButton
            android:layout_centerInParent="true"
            android:id="@+id/padelButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/padelbutton"
            android:backgroundTint="@color/Transparent">
        </ImageButton>
    </RelativeLayout>


    <ImageButton
        android:id="@+id/basketButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:src="@drawable/basketbutton"
        android:backgroundTint="@color/Transparent">
    </ImageButton>

    <ImageButton
        android:id="@+id/futbolButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:src="@drawable/futbolbutton"
        android:backgroundTint="@color/Transparent">
    </ImageButton>

</LinearLayout>